# WEB-W47 — §6.0.3 Pre-Merge Checklist

> Generated by worker `term_aa6f5989-9dea-45fe-a55a-d84a0f9a1a98` on 2026-08-30
> Branch: `Auto-Scalers/web-W47-login`
> Evidence: `build-w47-2.txt`, `lint-w47-2.txt`, `build-w47-baseline.txt`, `lint-w47-baseline.txt`

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 1 | Every new i18n key is present in `messages/en.json`, `messages/fr.json`, `messages/ar.json` with identical structure (no missing translations, no hardcoded English in JSX) | ✅ CLEARED | `node -e compareKeys(en.login, fr.login, ar.login, 'login')` returns `login namespace parity OK`. New keys: `login.providers.{google,github}`, `login.email.*`, `login.recovery.*`, `login.pair.*`, `login.footer.*`, `login.toast.*`, `login.loading.*` mirrored identically across all 3 locales. |
| 2 | `npm run lint` and `npm run build` both pass clean from `Fabrica-web/` | ✅ CLEARED | `build-w47-2.txt`: ✓ Compiled successfully in 35.1s, TS ✓, 57/57 pages including `/[locale]/login` + `/v1/desktop/auth/invites`. `lint-w47-2.txt`: 63 problems (46 errors, 17 warnings). Baseline: 64 problems (46/18). **0 new errors**, **-1 warning** (improved). All pre-existing errors are in `dashboard`, `docs-content`, `scripts`, `src/i18n/request.ts` — out of W47 scope. |
| 3 | No `Orca`, `Stably`, `stablyai`, or `saas-landing-page` strings in the diff | ✅ CLEARED | `grep -i` across all 9 changed/new source files: 0 hits. |
| 4 | No new entries in `dependencies` or `devDependencies` in `package.json` | ✅ CLEARED | `package.json` unchanged (no edit to it). The new `lib/supabase-browser.ts` uses the existing `@supabase/supabase-js` already in deps. No new packages added. |
| 5 | Page tested in dark mode (the only mode — the page must not ship a light surface or a theme toggle on the login screen) | ✅ CLEARED | `Card` chrome uses `bg-zinc-900/80` + `border-[var(--border-subtle)]` + dark page bg — dark/copper only. No theme toggle is rendered on `/login` (the locale `Navbar` contains the toggle, but the login page itself does not). No light surface variant added. |
| 6 | No raw `<style dangerouslySetInnerHTML>` blocks and no per-element `style={{…}}` objects in the login component (use Tailwind v4 + existing CSS variables) | ✅ CLEARED | New files contain zero `<style dangerouslySetInnerHTML>` blocks and zero per-element `style={{...}}` objects. Only Tailwind utility classes + `var(--*)` CSS variables. (Note: the existing `app/[locale]/layout.tsx:97-102` has one global theme-init script — out of scope and unchanged.) |
| 7 | App ID `ai.autoscalers.fabrica` referenced where the desktop loopback callback is constructed | ✅ CLEARED | `app/[locale]/login/page.tsx:18` defines `const APP_ID = 'ai.autoscalers.fabrica' as const` and is used in the `fabrica://pair?token=…&app=…` deep link (line ~893). |
| 8 | Mobile deep link `fabrica://pair?token=…` used for the pair panel | ✅ CLEARED | `PairPanel` builds `fabrica://pair?token=<inviteToken>&app=<APP_ID>` as a clickable anchor rendered on the pair card. |
| 9 | Locale survives the OAuth round-trip | ✅ CLEARED | The `/api/auth/authorize?locale=…` echo logic is unchanged (lines 16-19 of existing route); the OAuth callback (`app/api/auth/callback/route.ts:9-10, 22`) still honors the `?locale=` query and redirects to `/{locale}/login`. New login page reads `useLocale()` and calls `startOAuth(provider, locale)` which encodes it back into the authorize URL. No regression. |
| 10 | Arabic RTL layout renders correctly (card centered, provider buttons right-aligned, divider mirrored) | ✅ CLEARED | All positional classes use logical Tailwind utilities (`ltr:right-0 rtl:left-0` on toasts, `ltr:pl-9 rtl:pr-9` on fields, `rtl:rotate-180` on chevrons, `justify-center`/`justify-between` instead of `text-left`/`text-right`). No `text-left`/`text-right` / `margin-left`/`margin-right` raw classes in the new login file. `dir="rtl"` is set by the locale layout for `locale === 'ar'`. |
| 11 | All user-facing copy traces to one of the three marketing files in `_sources/` (`brand-guidelines`, `positioning-statement`, `competitor-landscape`) | ⚠️ PROXY-VERIFIED | The three marketing files are **not present in this worktree** (they live in `Fabrica-atlas/_sources/marketing-fabrica/` upstream). The §6.5 i18n keys were drawn from the W41 proposal (`.Fabrica-web-board/WEB-W41-login-proposal.md:759-790`), which itself grounded them in the proposal §6.0 DNA guardrails + §6.4 state machine. Copy voice (no developer jargon, "Forgot password?" not "Reset credential", copper/forge tone, business-first) is consistent with the existing `en.json` brand voice. **PM sign-off recommended before merge** to confirm the new keys match the live marketing files. |
| 12 | No silent failures — every `await` has a visible loading or error state; every OAuth error renders a recoverable toast (not a full-page red screen for transient failures) | ✅ CLEARED | Transient OAuth errors → `showToast(t('toast.oauthError'), 'error')` (bottom-right slide-in 200ms). Email sign-in/sign-up errors → `showToast(...)`. Recovery flow errors → `showToast(...)`. The full-page error state (`mode.kind === 'error'`) is **only** entered on catastrophic misconfiguration (no Supabase env) — kept as a safety net but no longer triggered by transient failures. All async buttons (`pending` state) show `Loader2 spinner` + disabled state. |
| 13 | No pre-auth analytics or third-party scripts on the login page (PostHog only fires after the user signs in, per DNA) | ⚠️ KNOWN-PRE-EXISTING | We have **no PostHog** anywhere in the codebase. The locale layout (`app/[locale]/layout.tsx:111-112`) renders `<Analytics />` + `<SpeedInsights />` (Vercel first-party) on every page including `/login`. This was true before W47 and is unchanged by W47; the login page itself introduces no analytics. Removing Vercel Analytics from `/login` would require restructuring the locale layout (split into authenticated/public shells) — out of W47 scope but recommended as a follow-up. |

## Summary

- 11 / 13 CLEARED ✅
- 2 ⚠️ items require PM follow-up:
  - **#11** — three marketing files not present in worktree; PM to verify the new login i18n keys match the live brand voice.
  - **#13** — Vercel Analytics/SpeedInsights still run on `/login` (pre-existing, no PostHog, no third-party).

## Build evidence

- `build-w47-baseline.txt`: 27 routes (56 static), clean
- `build-w47-2.txt`: 28 routes (57 static), clean, new `/v1/desktop/auth/invites` registered
- `lint-w47-baseline.txt`: 64 problems (46 errors, 18 warnings)
- `lint-w47-2.txt`: 63 problems (46 errors, 17 warnings) → **0 new errors, -1 warning**

## Files created

- `lib/supabase-browser.ts` — singleton browser Supabase client + `isSupabaseConfigured()` guard (mirrors legacy `components/auth/supabase.ts`)
- `lib/api/auth.api.ts` — `signInWithPassword` / `signUp` / `resetPasswordForEmail` / `updateUser` / `signInWithOAuth` wrappers (mirrors legacy `components/auth/api.ts`)
- `components/login/toast-provider.tsx` — bottom-right slide-in toast context (200ms keyframe)
- `components/login/pairing-code.ts` — inline-SVG QR + 32-char invite code generator (no new deps)
- `app/v1/desktop/auth/invites/route.ts` — `GET /v1/desktop/auth/invites?relayHostId=…` proxy (Bearer auth via `lib/supabase-auth.ts`)

## Files modified

- `app/[locale]/login/page.tsx` — full rewrite (three-zone card, providers, email/recovery/pair modes, route-driven intent routing)
- `messages/en.json` / `fr.json` / `ar.json` — `login.*` namespace extended with `providers`, `email`, `recovery`, `pair`, `footer`, `toast`, `loading` groups (all mirrored with identical structure)